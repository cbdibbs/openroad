# Native Bridge

This directory is reserved for native integrations such as:

- Bluetooth FTMS and ANT+ trainer support
- optional route matching acceleration
- content verification or unpack helpers

Rust is the expected default implementation language, exposed to Godot through GDExtension or a thin IPC layer.
