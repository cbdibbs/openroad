# Region Data

This directory stores versioned region-pack artifacts separately from code.

## Rules

- Region packs must be distributable as standalone artifacts.
- Each pack must include `manifest.json`, `attribution.json`, and route/scenery assets.
- Attribution and source metadata are required even for sample packs.
- Region packs can carry non-MIT data licensing terms independent of the repository code license.
